import React from 'react';
import styles from './Slice.module.css';

function Slice() {
    return (
      <Slice>
        {/* HTML del slice aquí */}
      </Slice>
    );
  }
  
  export default Slice;